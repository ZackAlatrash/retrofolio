node_modules/
dist/
.env
.env.local
.vercel
.DS_Store
playwright-report/
test-results/

# TypeScript incremental build cache: regenerated by every build.
*.tsbuildinfo

# Source art for generated assets. Kept out of the build: the shipped,
# downsampled versions live in public/.
art-src/

# Local agent tooling state (daemon pids, tuned policy caches). Machine-local,
# not project configuration.
.claude-flow/
.claude/proven-config.json
.claude/.proven-config-version
